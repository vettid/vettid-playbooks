{
  "name": "vettid-playbooks",
  "version": "0.1.0",
  "description": "VettID Privacy Playbooks \u2014 education and coaching content for vettid.org/playbooks",
  "license": "AGPL-3.0-or-later",
  "repository": {
    "type": "git",
    "url": "https://github.com/vettid/vettid-playbooks.git"
  },
  "engines": {
    "node": ">=20"
  },
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/sitemap.mjs",
    "preview": "astro preview",
    "check": "node scripts/validate.mjs",
    "deploy": "npm run build && npm run check && node scripts/deploy.mjs"
  },
  "dependencies": {
    "astro": "^7.2.2"
  }
}
